#include "head.h"

template<typename T>
class test {
public:
    const static int size = 2;
    T arr[size];
};

int main() {
    const int size = 1;
    int array[size];
    //fib(); 
    //jumpFloor();
    //jumpFloorExtra
    return 0;
}